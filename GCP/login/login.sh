#!/bin/bash
echo "Login GCP"
# export PROJECTID_GCP="${DESA_GOOGLE_PROJECT_ID}"
# echo "$DESA_SERVICE_ACCOUNTS" > /tmp/gcloud-service-account.json
# gcloud config set project ${PROJECTID_GCP}
# gcloud auth activate-service-account --key-file=/tmp/gcloud-service-account.json
